Project is started through:

Making the cs174c-project the current working directory

Running the 3 commands:
  npm install --save three
  npm install --save-dev vite
  npx vite

And following the localhost port. 